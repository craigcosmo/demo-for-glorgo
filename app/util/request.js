import axios from 'axios'
import MockAdapter from 'axios-mock-adapter'
import config from 'config'


export default function(){
	// return axios.create({})

	// let ax = axios.create()
	// let mock = new MockAdapter(ax)

	// // fetch all topics
	// mock.onPost(config.getTopicAPI).reply(function(){
	// 	return [200, topicCollectionMock]
	// })

	// // fetch post collection of post per topic
	// mock.onPost(config.getPostAPI).reply(function(){
	// 	return [200, postCollectionMock]
	// })
	
	// // login 
	// mock.onPost(config.loginAPI).reply(function(){
	// 	return [200, loginResponse]
	// })

	// // post message
	// mock.onPost(config.postMessageAPI).reply(function(){
	// 	return [200, postMessageResponse]
	// })

	// return ax

	
}
